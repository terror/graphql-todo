{
  "name": "graphql-todo",
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "scripts": {
    "start": "lerna run start --scope @graphql-todo/server --stream",
    "dev": "lerna run start --stream",
    "install": "(cd client && yarn install) && (cd server && yarn install)"
  }
}
